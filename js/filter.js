"use strict";import{menu}from "./menu.js";export const filter=($filterwrappers,filterObj,callback)=>{const $filterClearBtn=$filterwrappers.querySelector('[data-filter-clear]');const $filterValue=$filterwrappers.querySelector('[data-filter-value]');const $filterChip=$filterwrappers.querySelector('[data-filter-chip]');const $filterColorField=$filterwrappers.querySelector('[data-color-field]');const filterkey=$filterwrappers.dataset.filter;const newObj=filterObj;menu($filterwrappers,filterValue=>{$filterValue.innerHTML=filterValue;$filterChip.classList.add('selected');newObj[filterkey]=filterValue;callback(newObj)});$filterClearBtn.addEventListener("click",()=>{$filterChip.classList.remove('selected');$filterValue.innerText=$filterValue.dataset.filterValue;delete newObj[filterkey];callback(newObj)});$filterColorField?.addEventListener("change",function(){const filterValue=this.value.toLowerCase();$filterValue.innerText=filterValue;$filterChip.classList.add("selected");newObj[filterkey]=filterValue;callback(newObj)})}